{
  "name": "dogeai",
  "scripts": {
    "format": "prettier --write .",
    "check:format": "prettier --check .",
    "check:types": "pnpm -r check:types"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "pnpm": {
    "patchedDependencies": {
      "twitter-api-v2": "patches/twitter-api-v2.patch"
    }
  }
}
