{
  "name": "dogeai",
  "scripts": {
    "format": "prettier --write .",
    "check:format": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  }
}
